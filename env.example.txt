# ─────────────────────────────────────────────────────────────
# Aboki Environment Variables
# Copy this file to .env and fill in your values.
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────

# ── Core AI / LLM ──
GROQ_API_KEY=gsk_
GROQ_TRADING_API_KEY=             # Optional — separate key for trading calls (falls back to GROQ_API_KEY)

# ── Narrative Detection ──
# Add up to 3 Tavily API keys. Aboki uses them in order (1 → 2 → 3),
# automatically rotating to the next when one hits its credit limit.
# 3 free accounts × 1,000 credits each = 3,000 credits/month combined.
# At 1 call/hour, Aboki uses ~720 credits/month — plenty of headroom.
# Get keys at: tavily.com (free tier, no credit card needed)
TAVILY_API_KEY_1=tvly-diZOl9fVjsV
TAVILY_API_KEY_2=tvly-dev-7iS0K1zP
TAVILY_API_KEY_3=tvly-dev1bsQMpn

# ── Solana On-Chain Data ──
HELIUS_API_KEY=e04f4ea3e451856b

# ── Telegram Alerts ──
# Create a bot via @BotFather, get the token, then message @userinfobot for your chat ID
TELEGRAM_BOT_TOKEN=87Y5VW-QWg
TELEGRAM_CHAT_ID=6485
# ── Database ──
# Aboki uses SQLite by default (no setup needed). Uncomment POSTGRES_URL
# only if you want to switch to Postgres (e.g. AWS RDS).
SQLITE_FILE=./data/aboki.db       # Default SQLite path
# POSTGRES_URL=postgresql://user:password@host:5432/aboki

# ── AWS S3 Backup ──
# Used by scripts/backup.mjs to upload data/ snapshots to S3.
# Create a bucket in AWS S3 (e.g. "aboki-backups-yourname") and an
# IAM user with s3:PutObject / s3:GetObject / s3:ListBucket on that bucket.
BACKUP_S3_BUCKET=                 # e.g. aboki-backups-williams
AWS_ACCESS_KEY_ID=                # IAM user access key
AWS_SECRET_ACCESS_KEY=            # IAM user secret key
AWS_REGION=us-east-1              # Region your S3 bucket is in

# ── Internal ──
DAEMON_PROCESS=true               # Set by pm2/process manager; don't change
